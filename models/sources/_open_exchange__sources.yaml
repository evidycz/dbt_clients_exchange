version: 2

sources:
  - name: open_exchange
    database: "{{ var('open_exchange_database').get(target.name, target.database) }}"
    schema: "open_exchange"

    loader: DLT

    tables:
      - name: rates
