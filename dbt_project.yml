name: 'dbt_clients_exchange'
version: '0.1.0'

#  profile: 'dbt_clients_exchange'

model-paths: [ "models" ]
analysis-paths: [ "analyses" ]
test-paths: [ "tests" ]
seed-paths: [ "seeds" ]
macro-paths: [ "macros" ]
snapshot-paths: [ "snapshots" ]

clean-targets: # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"

models:
  dbt_clients_exchange:
    materialized: ephemeral
    bind: false
    sources:
      +schema: exchange_source
      +materialized: view
    marts:
      +schema: exchange
      +materialized: table
